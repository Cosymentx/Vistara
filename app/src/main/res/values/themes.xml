<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 基础应用主题 -->
    <style name="Theme.Obscura" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 主窗口背景色，防止启动时白屏 -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- 启动画面主题 -->
    <style name="Theme.Obscura.Splash" parent="Theme.SplashScreen">
        <!-- 设置启动画面背景色 -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- 设置启动画面图标 -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- 设置启动画面图标动画持续时间 -->
        <item name="windowSplashScreenAnimationDuration">300</item>
        <!-- 启动画面结束后的主题 -->
        <item name="postSplashScreenTheme">@style/Theme.Obscura</item>
    </style>

</resources>